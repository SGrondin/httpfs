<Window x:Class="Httpfs.Client.UI.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="ToolWindow"
        Title="" Height="150" Width="500">
    <Grid FocusManager.FocusedElement="{Binding ElementName=AnswerBox}">
        <DockPanel Margin="5">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" HorizontalAlignment="Right">
                <Button Name="OkButton" Width="60" Margin="0, 0, 5, 0">OK</Button>
                <Button Name="CancelButton" Width="60" Margin="5, 0, 0, 0">Cancel</Button>
            </StackPanel>
            <StackPanel>
                <TextBlock Name="InputQuestionBlock" Margin="0, 5, 0, 0"></TextBlock>
                <TextBox Name="AnswerBox" Margin="0, 10, 0, 10"></TextBox>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
